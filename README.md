# Python Project Template

This is a example python project, *that can be used as a template*. It will allow pytest to check
whether program is running as expected and use sphinx to create documentation.
It will also use GitHub actions for running these tests.
